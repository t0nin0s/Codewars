function tennisBalls(detritus) {
  const balls = detritus.filter((item) => item === 58).reduce((acc, value) =>
    total = acc + value, 0);
  return { weight: balls };
}

module.exports = tennisBalls;